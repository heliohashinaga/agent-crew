# No-Tasks Fixture — Guided Edit

A speckit folder that omits `tasks.md`, exercising the error path where the
adapter cannot derive an executable subtask list (FR-003).

## Functional Requirements

- FR-001: Support guided, safe file edits.

## Non-Goals

- No autonomous multi-file rewriting.
